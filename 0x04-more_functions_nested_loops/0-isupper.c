#include <stdio.h>

/**
 * _isupper - script that checks if c is uppercase
 * @c: value to be checked
 * Return: 0
 */

int _isupper(int c)
{
	int i;

	for (i = 'A'; i <=  'Z'; i++)

		if (c == 'C')
		{
			return (1);
		else

	return (0);
	}
}
