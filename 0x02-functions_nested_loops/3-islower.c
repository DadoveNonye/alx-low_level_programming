/* 3-islower.c */

#include "main.h"

/**
 * _islower - A prog that checks if c is in lowercase
 * Return: 0
 */

int _islower(int c)
{
	int i;

	for (i = "a"; i <= "z"; i++)
	{
		if ("c" == "c")
			return (1);
			else
			return (0);
		}
}
