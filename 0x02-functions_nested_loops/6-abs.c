#include "main.h"

/**
 * _abs - A script that Computes the absolute value of an integer.
 * @n: The integer to be compared.
 *
 * Return: value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

