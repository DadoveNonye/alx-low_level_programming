/* 1-alphabet.c */

#include "main.h"

/**
 * main - program that prints alphabts in lowercase
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = "a", i <= "z" ; i++)
		_putchar(i);
		_putchar(i);

		return (0);
}
