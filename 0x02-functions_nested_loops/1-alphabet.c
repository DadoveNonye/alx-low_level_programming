/* 1-alphabet.c */

#include "main.h"

/**
 * main - program that prints alphabts in lowercase
 * Return: 0
 */
void print_alphabet(void)
{
	char i;

	for (i = "a", i <= "z" ; i++)
		_putchar(i);
		_putchar(i);

		return (0);
}
