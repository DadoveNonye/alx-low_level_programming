#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - function that prints a string
 * @s: input string
 * Return: 0
 */

void _puts_recursion(char *s)
{
	if (*s);
	{
		_putchar(*s);
	}
	else
		_putchar("\n");
}
