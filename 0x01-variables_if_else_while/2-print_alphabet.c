/*2-print_alphabet.c*/

#include<stdio.h>

/**
 * main -  program that prints the alphabet in lowercase,
 *  followed by a new line.
 *  Return: always o.
 */

int main(void)
{
	char alpha
		for (alpha = "a"; alpha <= "z"; alpha++);
			putchar(alpha);
			putchar("\n")
			return(0)	
}
